<form method="post" action="{{ path('app_tablee_delete', {'idTable': tablee.idTable}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tablee.idTable) }}">
    <button class="btn">Delete</button>
</form>
