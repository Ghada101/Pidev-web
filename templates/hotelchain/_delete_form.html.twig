<form method="post" action="{{ path('app_hotelchain_delete', {'idHotelchain': hotelchain.idHotelchain}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotelchain.idHotelchain) }}">
    <button class="btn btn-danger">Delete</button>
</form> 
