{% extends 'BackOffice/backfinal.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <h1>Create new Category</h1>
{{form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}

   <div class="form-group">
    <label for="name">Name</label>
      {{ form_widget(form.nameCategory , {'attr' : {'class' : 'form-control'  }}  ) }}
         
        {{ form_errors(form.nameCategory ,  {'attr' : {'class' : 'text-danger'  }} ) }}
  </div>
  <div class="form-group">
    <label for="name">description</label>


      {{ form_widget(form.descriptionCategory , {'attr' : {'class' : 'form-control'  }}  ) }}
                                
        {{ form_errors(form.descriptionCategory ,  {'attr' : {'class' : 'text-danger'  }}) }}
  </div>
   <button class="btn btn-primary mb-2">{{ button_label|default('Save') }}</button>

  {{ form_end(form) }} 

  
   

    <a href="{{ path('app_category_index') }}">back to list</span>
      <i class="fa fa-angle-right"></i></a>
{% endblock %}
