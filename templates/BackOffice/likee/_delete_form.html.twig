<form method="post" action="{{ path('app_likee_delete', {'likeid': likee.likeid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ likee.likeid) }}">
    <button class="btn">Delete</button>
</form>
