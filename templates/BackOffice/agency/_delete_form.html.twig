<form method="post" action="{{ path('app_agency_delete', {'idAgency': agency.idAgency}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agency.idAgency) }}">
    <button class="btn">Delete</button>
</form>
