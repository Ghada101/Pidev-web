{% extends 'FrontOffice/frontfinal.html.twig' %}

{% block title %}Reservetable{% endblock %}

{% block body %}
    <h1>Reservetable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>startdate</th>
                <td>{{ reservetable.startdate ? reservetable.startdate|date('Y-m-d , h:m') : ''}}</td>
            </tr>
            <tr>
                <th>enddate</th>
                <td>{{ reservetable.enddate ? reservetable.enddate|date('Y-m-d , h:m') : ''}}</td>
            </tr>
            <tr>
                <th>Table</th>
                <td>{{ reservetable.idTable}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reservetable_index') }}">back to list</a>

    <a href="{{ path('app_reservetable_edit', {'idreserve': reservetable.idreserve}) }}">edit</a>

    {{ include('reservetable/_delete_form.html.twig') }}
{% endblock %}
