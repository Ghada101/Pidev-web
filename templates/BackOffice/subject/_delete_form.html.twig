<form method="post" action="{{ path('app_subject_deleteBack', {'subjectId': subject.subjectId,'IdTopic':subject.topic.topicId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subject.subjectId) }}">
    <button class="btn btn-danger btn-circle"><i class="fas fa-trash"></i></button>
</form>
