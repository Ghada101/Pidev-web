<form method="post" action="{{ path('app_rent_delete', {'idrent': rent.idrent}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rent.idrent) }}">
    <button class="btn">Delete</button>
</form>
