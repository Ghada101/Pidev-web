<form method="post" action="{{ path('app_dislikee_delete', {'dislikeid': dislikee.dislikeid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dislikee.dislikeid) }}">
    <button class="btn">Delete</button>
</form>
