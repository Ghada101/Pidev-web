<form method="post" action="{{ path('app_car_delete', {'idCar': car.idCar}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.idCar) }}">
    <button class="btn">Delete</button>
</form>
